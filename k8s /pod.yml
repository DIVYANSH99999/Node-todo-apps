apiVersion: v1
kind: Pod
metadata:
  name: node-pod
  namespace: node-app

spec:
  containers:
    - name: node-container
      image: divyanshpaliwal/node-app-demo
      ports:
        - containerPort: 8000
